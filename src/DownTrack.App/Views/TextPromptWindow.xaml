<Window x:Class="DownTrack.Views.TextPromptWindow"
        FlowDirection="{Binding FlowDirection, Source={x:Static loc:LocalizationService.Instance}}"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:DownTrack.Infrastructure.Localization"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Width="520" Height="250" WindowStyle="None" AllowsTransparency="True"
        Background="Transparent">
    <Border Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1" CornerRadius="18" Padding="26">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock x:Name="TitleText" FontSize="22" FontWeight="SemiBold"/>
            <TextBlock x:Name="MessageText" Grid.Row="1" Margin="0,8,0,16"
                       Foreground="{DynamicResource MutedTextBrush}" TextWrapping="Wrap"/>
            <TextBox x:Name="ValueBox" Grid.Row="2"/>
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0">
                <Button Content="{loc:Loc Key=Common.Cancel}" Style="{StaticResource GhostButton}" Click="Cancel_Click"/>
                <Button Content="{loc:Loc Key=Common.Continue}" Style="{StaticResource PrimaryButton}" Margin="8,0,0,0" Click="Continue_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
